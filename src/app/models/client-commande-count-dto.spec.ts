import { ClientCommandeCountDTO } from './client-commande-count-dto';

describe('ClientCommandeCountDTO', () => {
  it('should create an instance', () => {
    expect(new ClientCommandeCountDTO()).toBeTruthy();
  });
});
